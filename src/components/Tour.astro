---
import { Markdown } from 'astro/components';
import Counter from './Counter.jsx';
---
<article>


  <!-- This Counter will only load the client side JS when it is visible on the page -->
  <Counter client:visible />

  <section>
    <p>
We leverage AI-powered analytics to evaluate therapy and coaching sessions in real-time. Therapists and coaches gain actionable insights to optimize their practice, while clients receive personalized feedback for a more effective healing journey. Improve outcomes, maximize engagement, and build a more impactful practice—all in one intuitive platform
    </p>
  </section>
    
</article>

<style>
    article {
        padding-top: 2em;
        line-height: 1.5;
    }
    
    section {
        display: flex;
        flex-direction: column;
        gap: 1em;
        max-width: 70ch;
    }

    pre,
    code {
        font-family: var(--font-mono);
        background: var(--color-light);
        border-radius: 4px;
    }

    pre {
        padding: 1em 1.5em;
    }

    .tree {
        line-height: 1.2;
    }

    code:not(.tree) {
        padding: 0.125em;
        margin: 0 -0.125em;
    }
</style>
